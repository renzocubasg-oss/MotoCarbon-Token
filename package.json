{
  "name": "motocarbon-token",
  "version": "1.1.0",
  "description": "Blockchain-based platform for carbon tokenization of electric mototaxis with an interactive web application.",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "node scripts/deploy.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
